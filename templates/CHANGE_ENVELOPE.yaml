schemaVersion: 2
ownerDecision: DEC-XXXX
workItem: FEAT-123
riskClass: R2
protocolLevel: P1               # P0 | P1 | P2; смешанный scope — высший
predecessor:
  state: OWNER_ACCEPTED
  receiptSha256: <sha256>
rollbackAnchor:
  state: VERIFIED
  type: GIT_TAG | DB_SNAPSHOT | INFRA_SNAPSHOT | ARCHIVE
  sha256: <sha256>
scope:
  workspace: <one allowed root>
  data: SYNTHETIC_ONLY | ANONYMIZED | PRODUCTION_READONLY
  budget: ZERO_SPEND | <limit>
allowedPaths:
  - src/<bounded-area>
  - tests/<bounded-area>
  - docs/<bounded-area>
allowedEffects:
  - LOCAL_LOOPBACK
  - CI_PIPELINE
forbiddenEffects:
  - PRODUCTION_DATA
  - EXTERNAL_NETWORK
  - PROVIDER_SELECTION
  - SPEND
  - PILOT
  - PRODUCTION
  - DEPLOYMENT
requiredTests:
  - LINT
  - TYPECHECK
  - UNIT
  - INTEGRATION
  - E2E
  - SAST
  - DEPENDENCY_SCAN
  - SECRET_SCAN
  - SBOM
  - REPRODUCIBLE_BUILD
nextWorkItem:
  automaticallyOpened: false
